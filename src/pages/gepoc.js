import * as React from "react"

import NavBar from '../components/navbar';

const GepocPage = () => {
  return (
    <main>
        <NavBar />
        <h1>GEPOC</h1>
    </main>
  )
}

export default GepocPage

export const Head = () => <title>GEPOC</title>
